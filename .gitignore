# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
*.key

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# secrets — NEVER commit these
.env
.env.*
!.env.example
.env.local
.env.development
.env.production
.env.test

# generated vector indexes (re-create with `npm run ingest`)
/data/index/*.json

# trial protocol PDFs (may be confidential — add to repo only if intended)
/data/pdfs/*.pdf

# typescript
*.tsbuildinfo
